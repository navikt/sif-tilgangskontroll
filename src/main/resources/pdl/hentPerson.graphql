query($ident: ID!){
    hentPerson(ident: $ident) {
        folkeregisteridentifikator(historikk: false){
            identifikasjonsnummer
        }
        adressebeskyttelse {
            gradering
        }
        code
    }
}
